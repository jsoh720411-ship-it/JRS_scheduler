<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jasper.scheduler.mapper.AdminUserMapper">

    <select id="findPasswordHashById" resultType="string">
        SELECT password_hash FROM dbo.ADMIN_USER
        WHERE admin_id = #{adminId} AND is_active = 1
    </select>

    <update id="updateLastLogin">
        UPDATE dbo.ADMIN_USER SET last_login_at = SYSDATETIME()
        WHERE admin_id = #{adminId}
    </update>

</mapper>
